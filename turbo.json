{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "outputMode": "errors-only"
    },
    "lint": {
      "outputMode": "errors-only"
    },
    "format": {
      "outputMode": "errors-only"
    }
  }
}
