/** @type {import('prettier').Config} */
const config = {
	endOfLine: 'lf',
	printWidth: 120,
	quoteProps: 'as-needed',
	semi: true,
	singleQuote: true,
	trailingComma: 'none'
};

export default config;
